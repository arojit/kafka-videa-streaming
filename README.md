# kafka-videa-streaming
``` 
Kafka producer sends streaming data from CV2 Camera or Video file to a kafka topic.
There is a server socket which is listening to the perticuler kafka topic using kafka consumer.
There is a html file or client socket which is connecting to the server socket and receiving and displaying the streaming data to the browser.
```
